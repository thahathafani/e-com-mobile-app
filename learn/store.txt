import 'package:flutter/material.dart';

class StoreScreen extends StatelessWidget {
  const StoreScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(  // Fixed incorrect TAppBar usage
        title: Text('Store', style: Theme.of(context).textTheme.headlineMedium),
        actions: [
          IconButton(
            icon: Icon(Icons.shopping_cart), 
            onPressed: () {},
          ),
        ],
      ),
      body: NestedScrollView(
        headerSliverBuilder: (context, innerBoxIsScrolled) { // Fixed parameter syntax
          return [
            SliverAppBar(
              automaticallyImplyLeading: false,
              pinned: true,
              floating: true,
              backgroundColor: Theme.of(context).brightness == Brightness.dark ? Colors.black : Colors.white,
              expandedHeight: 448,
              flexibleSpace: Padding(
                padding: EdgeInsets.all(16.0), // Replaced TSizes.defaultSpace
                child: ListView(
                  shrinkWrap: true,
                  physics: const NeverScrollableScrollPhysics(),
                  children: const [
                    SizedBox(height: 10.0), // TSizes.spaceBtItems replaced
                    TSearchContainer(text: 'Search in Store', showBorder: true),
                    SizedBox(height: 20.0), // TSizes.spaceBtwSections replaced
                  ],
                ),
              ),
            ),
          ];
        },
        body: Column(
          children: [
            const SizedBox(height: 20.0),  // Fixed duplicate SizedBox
            TSectionHeading(title: 'Featured Brands', onPressed: () {}),
            const SizedBox(height: 10.0), // Fixed extra duplicate section heading
            TRoundedContainer(
              padding: const EdgeInsets.all(8.0), // TSizes.sm replaced
              showBorder: true,
              backgroundColor: Colors.transparent,
              child: Row(
                children: [
                  Container(
                    width: 56,
                    height: 56,
                    padding: const EdgeInsets.all(8.0), // TSizes.sm replaced
                    decoration: BoxDecoration(
                      color: Theme.of(context).brightness == Brightness.dark ? Colors.black : Colors.white,
                      borderRadius: BorderRadius.circular(100),
                    ),
                    child: Image.asset('assets/icons/cloth.png', color: Colors.black),
                  ),
                ],
              ),
            ),
            const SizedBox(height: 10.0),
            Expanded(
              child: GridView.builder(
                itemCount: 4,
                gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(
                  crossAxisCount: 2,
                  childAspectRatio: 4.0,
                ),
                itemBuilder: (context, index) {
                  return GestureDetector(
                    onTap: () {},
                    child: TRoundedContainer(
                      padding: const EdgeInsets.all(8.0),
                      showBorder: true,
                      backgroundColor: Colors.transparent,
                      child: Row(
                        children: [
                          Flexible(
                            child: CircleAvatar(
                              backgroundImage: AssetImage('assets/icons/cloth.png'),
                              backgroundColor: Colors.transparent,
                            ),
                          ),
                          const SizedBox(width: 8.0),
                          Expanded(
                            child: Column(
                              mainAxisSize: MainAxisSize.min,
                              crossAxisAlignment: CrossAxisAlignment.start,
                              children: [
                                Text(
                                  'Nike',
                                  style: TextStyle(fontSize: 16.0, fontWeight: FontWeight.bold),
                                ),
                                Text(
                                  '256 products available',
                                  overflow: TextOverflow.ellipsis,
                                  style: Theme.of(context).textTheme.bodySmall,
                                ),
                              ],
                            ),
                          ),
                        ],
                      ),
                    ),
                  );
                },
              ),
            ),
          ],
        ),
      ),
    );
  }
}
